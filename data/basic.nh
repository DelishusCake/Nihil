// See basic.c for the output from this file
do_print :: (lim:u32)
{
	for (let var i:u32 = 0; i < lim; i = i + 1)
	{
		printf("%d ", i);
	}
	printf("\n");
}

main :: () -> i32
{
	do_print(100);
	return 0;
}